---
# tasks file for roles/suse_moni

- name: prometheus install
  zypper:
    name: golang-github-prometheus-prometheus
    state: latest
    update_cache: yes

- name: start prometheus
  service:
    name: prometheus
    state: started
